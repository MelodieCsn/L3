#include<iostream>
#include<string>
using namespace std;
#include"vers.h"
#include"strophe.h"
int main(){
    // Strophe *s = new Strophe();
    // s->saisie(cin);
    // s->affiche(cout);
    return 0;
}